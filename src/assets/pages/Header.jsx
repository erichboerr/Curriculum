import React from 'react';
import NavBar from '../components/NavBar.jsx';

const Header = () => {
    return (
        <header>
          <NavBar />
        </header>
    );
}

export default Header;
